import '../css/style.css'
import '../css/style.scss'


/* let allNumbersInTimer = document.querySelectorAll('.time-block__number')
let second;
let minute;
let hour;
let day;

allNumbersInTimer.forEach(item => {
    if (item.id == 'second') {
        second = item
    }
    else if (item.id == 'minute') {
        minute = item
    } else if (item.id == 'hour') {
        hour = item
    } else if (item.id == 'day') {
        day = item
    }
})

allNumbersInTimer.forEach(item => {
    allNumbersInTimer = Array.from(allNumbersInTimer)
    second--
})
 */